[build]
  command = "npm run build"
  publish = "build"

[dev]
  command = "npm start"
  port = 3000

[context.production.environment]
  REACT_APP_BACKEND_URL = "https://moodsync-backend.onrender.com"
